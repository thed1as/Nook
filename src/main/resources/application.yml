spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nook_db
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
    show-sql: true
  flyway:
    enabled: true
#    baseline-on-migrate: true
  springdoc:
    api-docs:
      path: /api-docs
    swagger-ui:
      path: /swagger
  security:
    secret-key: Wm85F524PED7mf2-EbwfUuGV96u6a5HxSaa4nZXI98A
    expiration: 3600000

minio:
  url: http://localhost:9000
  access-key: minio
  secret-key: minio123
  bucket: listing-images

app:
  booking:
    cancellation-window-days: 2